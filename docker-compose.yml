services:
  stewy-os:
    build: .
    container_name: stewy-os
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8020:8000"
    volumes:
      - stewy_data:/app/data
    extra_hosts:
      - "host.docker.internal:host-gateway"

volumes:
  stewy_data:
